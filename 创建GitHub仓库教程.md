# 📚 GitHub 仓库创建完整教程（新手向）

这是一份面向完全没有 Git 经验的新手教程。

---

## 🌟 你需要做什么

是的，你需要创建一个新的 GitHub 仓库来托管这个渲染器。这样做的好处是：

1. **免费托管** - GitHub Pages 免费提供网页托管
2. **跨设备访问** - 任何设备都能用同一个链接
3. **版本管理** - 方便更新素材

---

## 📝 方式一：网页上传（最简单，推荐新手）

### 第一步：注册/登录 GitHub

1. 打开 https://github.com
2. 如果没有账号，点击 **Sign up** 注册
3. 如果有账号，点击 **Sign in** 登录

### 第二步：创建新仓库

1. 登录后，点击右上角的 **+** 按钮
2. 选择 **New repository**
3. 填写信息：
   - **Repository name**: `galgame-renderer`（仓库名称）
   - **Description**: `SillyTavern Galgame Renderer`（可选描述）
   - **✅ 选择 Public**（公开，GitHub Pages 免费版需要）
   - **✅ 勾选 "Add a README file"**
4. 点击绿色按钮 **Create repository**

### 第三步：上传文件

1. 进入刚创建的仓库页面
2. 点击 **Add file** → **Upload files**
3. 将 `galgame-renderer` 文件夹里的所有内容拖入上传区：
   - `index.html`
   - `style.css`
   - `renderer.js`
   - `config.js`
   - `characters` 文件夹（包含你的立绘）
   - `backgrounds` 文件夹（包含你的背景）
   - `audio` 文件夹（如果有的话）
4. 在下方 **Commit changes** 区域：
   - 填写描述（如："Initial upload"）
   - 点击绿色按钮 **Commit changes**

### 第四步：启用 GitHub Pages

1. 在仓库页面，点击顶部的 **Settings**（齿轮图标）
2. 在左侧菜单找到 **Pages**
3. 在 **Source** 下：
   - 选择 **Deploy from a branch**
   - Branch 选择 **main**
   - 文件夹选择 **/ (root)**
4. 点击 **Save**
5. 等待 1-2 分钟，刷新页面
6. 你会看到：
   ```
   Your site is live at https://你的用户名.github.io/galgame-renderer/
   ```

### 第五步：测试

访问上面的链接，应该能看到 Galgame 渲染器界面。

---

## 💻 方式二：Git 命令行（进阶）

如果你想用命令行（更方便后续更新素材），按以下步骤：

### 安装 Git

**Windows**：
1. 下载：https://git-scm.com/download/win
2. 安装，一路 Next 即可

**Mac**：
```bash
brew install git
```

### 配置 Git（首次使用）

打开终端/命令提示符，执行：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 初始化并推送

```bash
# 进入 galgame-renderer 文件夹
cd galgame-renderer

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 创建第一个提交
git commit -m "Initial commit"

# 设置默认分支名
git branch -M main

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/galgame-renderer.git

# 推送到 GitHub
git push -u origin main
```

### 后续更新素材

每次添加新素材后：

```bash
git add .
git commit -m "添加新角色：xxx"
git push
```

---

## 🔧 常见问题

### Q: 提示权限错误？

需要配置 GitHub 认证，推荐使用 Personal Access Token：
1. GitHub → Settings → Developer settings → Personal access tokens
2. 生成新 token，勾选 repo 权限
3. 推送时用 token 作为密码

### Q: 上传大文件失败？

GitHub 限制单个文件 100MB。如果素材太大：
1. 压缩图片质量
2. 或使用 Git LFS（大文件存储）

### Q: Pages 没有生效？

1. 确认仓库是 Public
2. 确认 Pages 设置正确（main branch，/ root）
3. 等待几分钟让 GitHub 构建

---

## 📱 完成后配置 SillyTavern

获得 GitHub Pages 链接后，在 SillyTavern 添加正则规则：

**替换中的 URL**：
```
https://你的用户名.github.io/galgame-renderer/?scene=$1&char=$2&exp=$3&text=$4
```

把 `你的用户名` 换成你实际的 GitHub 用户名即可。

---

## ✅ 检查清单

- [ ] GitHub 账号已创建
- [ ] 仓库已创建（Public）
- [ ] 文件已上传
- [ ] GitHub Pages 已启用
- [ ] 可以访问 Pages 链接
- [ ] SillyTavern 正则已配置
- [ ] 测试成功
