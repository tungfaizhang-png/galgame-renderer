# 🔧 解决 GitHub Pages 404 错误

## 问题原因

你的文件结构是：
```
仓库根目录/
└── GalGame-Renderer/       ← 多了一层文件夹！
    └── GalGame-Renderer/   ← 又多了一层！
        ├── index.html
        └── ...
```

但 GitHub Pages 需要：
```
仓库根目录/
├── index.html              ← 直接在根目录
├── style.css
├── renderer.js
├── config.js
└── ...
```

---

## 解决方法

### 方法一：删除仓库重新上传（最简单）

1. **删除当前仓库**
   - 进入仓库 Settings → 滚动到最下方
   - 点击 "Delete this repository"
   - 确认删除

2. **重新创建仓库**
   - 创建新仓库，名称 `galgame-renderer`
   - 选择 Public

3. **正确上传文件**
   - 点击 "Add file" → "Upload files"
   - **直接把文件拖入**（不要整个文件夹）：
     - `index.html`
     - `style.css`
     - `renderer.js`
     - `config.js`
     - `README.md`
     - 等等...
   - 提交更改

4. **启用 Pages**
   - Settings → Pages → main branch → Save

---

### 方法二：修改 Pages 路径

或者你可以直接修改访问路径：

由于你的文件在 `GalGame-Renderer/GalGame-Renderer/` 下，你可以访问：

```
https://tungfaizhang-png.github.io/galgame-renderer/GalGame-Renderer/GalGame-Renderer/
```

但这个路径太长，不推荐。建议用方法一。

---

## 正确的上传方式

上传时，**不要**拖入整个文件夹，而是：
1. 打开本地的 `galgame-renderer` 文件夹
2. 选中里面的**所有文件和子文件夹**
3. 拖入 GitHub 的上传区

这样文件就会直接在仓库根目录了。
